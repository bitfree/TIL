when you can not find bfd.h install binutils-dev 
linux kernel source
	cd tools and make net sometimes it ocuurs bison,flex error
	sudo apt-get install bison flex
	you also need libreadline-dev
	sudo apt-get install libreadline-dev

The program 'perf' is currently not installed. You can install it by typing:
sudo apt-get install linux-tools-common

can not find -ldw 
sudo apt-get install libdw-dev




